import * as React from 'react'
import { styled, css } from '../../styles'

export const Link = styled('a', {
  cursor: 'pointer',
  fontFamily: '$mainFont',
  textDecoration: 'none',
  color: '$brandGreen',
  fontWeight: 200,
  fontSize: 16,
  letterSpacing: -0.25,
})
